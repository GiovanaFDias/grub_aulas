#include <bits/stdc++.h>
using namespace std;

int main(){

	vector<int> dinamico;
	for(int i = 0; i < 5; i++){
		int provisoria;
		cin >> provisoria;
		dinamico.push_back(provisoria);
	}
	
	for(int i = 0; i < 5; i++){
		cout << dinamico[i] << ' ';
	}
	
	return 0;
}
